<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
	<div class="container-md" id="main-nav">
		{# <a class="navbar-brand" href="#">
			<img src="{{ asset('build/images/EXTERNATIC-LOGO.png') }}" alt="" width="170">
		</a> #}
		<a class="navbar-brand" href="{{ path('home_index') }}">Lionel Da Rosa</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" target="_blank" href="#">Portfolio</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('home_contact') }}">Contact Me</a>
				</li>
			</ul>
		</div>
        {% if app.user %}
			<div class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
					{{ app.user.email }}
				</a>
				<div class="dropdown-menu" >
					<a class="dropdown-item" href="{{ path('message_index') }}">Messages</a>
					<a class="dropdown-item" href="{{ path('project_index') }}">My Projects</a>
					<div class="dropdown-divider"></div>
					<a class="dropdown-item" href="{{ path('app_logout') }}">LOGOUT</a>
				</div>
			</div>
        {% else %}
            <a href="{{ path('app_login') }}">LOGIN</a>
        {% endif %}
	</div>
</nav>